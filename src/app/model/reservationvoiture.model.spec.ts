import { Reservationvoiture } from './reservationvoiture.model';

describe('Reservationvoiture', () => {
  it('should create an instance', () => {
    expect(new Reservationvoiture()).toBeTruthy();
  });
});
